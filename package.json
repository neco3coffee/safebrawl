{
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "npm --prefix apps/web run lint && npm --prefix apps/api run lint && npm --prefix apps/app run lint && npm --prefix packages run lint"
  }
}
